tls_server_config:
  cert_file: /etc/prometheus/config/certs/prometheus.crt
  key_file: /etc/prometheus/config/certs/prometheus.key

basic_auth_users:
  ${PROMETHEUS_USERNAME}: ${PROMETHEUS_PASSWORD_HASH}